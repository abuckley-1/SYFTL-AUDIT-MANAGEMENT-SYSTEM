name: Master Memory Sync

on:
  push:
    branches: [ main ]
  workflow_dispatch: # This line is critical for the website trigger

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Sync Confirmation
        run: |
          echo "Master Memory Sync Successful."
          echo "Supertram Audit Management System is now synchronized."
